<!--<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @lesson.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @lesson.description %>
</p>

<p>
  <strong>Slug:</strong>
  <%= @lesson.slug %>
</p>

<p>
  <strong>Mark as complete here:</strong>
  <%= @lesson.slug %>
</p>-->

<div class="fond" id="smoothscroll">
  <div class="button-flip">
      <a class="active" href="#text">
         <button type="button" class="btn" href="#text">More info</button>
      </a>
  </div>
</div>

<%= react_component(@lesson.name, prerender: false) %>

<%= render :partial => 'completed_lessons/form' %>

<%= link_to 'Edit', edit_lesson_path(@lesson) %> |
<%= link_to 'Back', lessons_path %>
